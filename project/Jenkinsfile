pipeline {
     agent any
     stages {
          stage("checkout") {
               steps {
                 git url: 'https://github.com/mekulaventure/Jenkinsdockerproject.git'
               }
          }
          stage('Second Stage') {
               steps {
                    sh 'touch file1'
                    echo 'Step 3. Third time Hello'
               }
          }
     }
}
